# CLI (offline tools)

Minimal decade-stable utilities:
- `canon.py` canonicalize JSON
- `id.py` compute contribution_id = sha256(canonical_json)
- `bundle.py` create a proof bundle skeleton

No heavy dependencies.
