# Changelog

## 2.0.0 (2026-01-06)
- Decade-stable, artifact-first architecture
- Protocol v2: contributions, accepted-set, proof bundles
- Two-lane index model (stable + sandbox)
- Offline signing workflow + merkle root support
- Expanded schemas (incident, trust-state, proof bundle)
- Worker v2 endpoints + storage adapters
- Deterministic build pipeline + verifier CLI
